# discover only unittest
coverage run -m unittest discover tests

# specific file
coverage run -m pytest -q tests/test_qcals_exec.py

# discover both unittest and pytest, all files in tests folder (better)
coverage run -m pytest -q tests

coverage report -m
coverage report
